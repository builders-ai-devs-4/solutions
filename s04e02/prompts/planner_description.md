Planner agent analyzes collected data and configures the wind turbine scheduler.
It identifies storm periods and optimal production windows, generates unlock codes in parallel,
submits batch configuration, runs turbine check, and finalizes with done action.
Returns: success flag {FLG:...} on success.
