Complete the following tasks in the OKO system:

1. Change the classification of the Skolwin incident report to animals.
2. Find the task related to Skolwin, mark it as done, and update its content to mention animals (e.g. beavers).
3. Ensure an incident report appears for the city of Komarowo — detected human movement in the area.
4. Call the done action after all changes are confirmed.

---

Before planning any edits:
- You MUST call the explorer first to get the verbatim raw API help response.
- Do NOT assume which fields, actions, or allowed values exist — derive everything from the help response.
- Do NOT assume creation is unsupported — check the help first.
- List ALL records on the relevant pages, not only those matching a target city name.

Maximum retries per failed edit: $MAX_EDIT_RETRIES